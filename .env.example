# Server-side API proxy target (Next.js rewrites). Local dev uses the default below.
BACKEND_INTERNAL_URL=http://localhost:5000

# Production example:
# BACKEND_INTERNAL_URL=https://your-backend.onrender.com
